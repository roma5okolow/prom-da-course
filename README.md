# NER Project: LSTM-извлечение сущностей

Данный проект представляет собой систему для распознавания именованных сущностей (NER). В основе лежит модель **LSTM**, экспортированная в формат **ONNX** для обеспечения высокой скорости работы на CPU.

## 🏗 Архитектура системы

Проект развернут с помощью Docker Compose и включает следующие сервисы:

* **Backend (FastAPI):** Обрабатывает API-запросы, выполняет токенизацию текста и запускает модель.
* **Frontend (Streamlit):** Веб-интерфейс для ввода текста и визуализации результатов в виде таблиц.
* **Monitoring (Prometheus & Grafana):** Сбор и визуализация метрик производительности (latency, RPS).
